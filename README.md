# Prześwity Editor

Stage 1 collaborative publishing editor. See `plans/start.md` for the full plan.

## Quickstart (dev)

```
cp backend/.env.example backend/.env
just dev
```

Backend on `:8080`, frontend on `:3000`, Postgres on `:5432`.
