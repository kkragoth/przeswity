// Re-export for back-compat — split into state.ts, matching.ts, plugin.ts.
export type { Match, FRState } from '@/editor/tiptap/find/state';
export { findReplaceKey, FindReplace } from '@/editor/tiptap/find/plugin';
